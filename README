.quickstart

* get rails: http://www.rubyonrails.org/down
** am besten ruby-gems holen und dann per sudo gem install rails installieren

* cp config/database.example config/database.yml # sqlite passt scho. muss halt das rails plugin installiert sein
* ruby script/migrate # macht datenbank modell
* ruby script/server
* http://localhost:3000