<div class="span-24 lrborder">
  <div class="span-23 push-1 last">
  <h1>Open Orders</h1>
  <ul>
  <% for order in @orders %>
    <li><%= link_to h(order.name), :action => :send_order, :id => order %>
  <% end %>
  </ul>
  </div>
</div>
