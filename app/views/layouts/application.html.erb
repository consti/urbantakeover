<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN"
       "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">

<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en">
<head>
  <meta http-equiv="content-type" content="text/html;charset=UTF-8" />
  <title>ut:// <%= controller.controller_name %>: <%= controller.action_name %></title>
  <%= stylesheet_link_tag 'scaffold' %>
</head>
<body>

<div style="float:right;">
  <% if logged_in? %>
    <% if current_user.is_admin? %>
      y0
    <% else %>
     hello
    <% end %>
    <%= h current_user.login %>
    |
    <%= link_to 'my claims', :controller => :claims, :action => :my %>
    |
    <%= link_to 'log out', :controller => :user, :action => :logout %>
  <% else %>
    <%= link_to 'log in', :controller => :user, :action => :login %>
    |
    <%= link_to 'sign up', :controller => :user, :action => :signup %>
  <% end %>
</div>

<h1 class="title"><%= link_to 'ut', root_url %></h1>

<p style="color: green"><%= flash[:notice] %></p>

<%= yield  %>

</body>
</html>
